const array1 = [5, 12, 8, 130, 44];

let index = 2;
console.log(`using an index of ${index} the item return is ${array1.at(index)}`);

index = -2;
console.log(`using an index of ${index} the item return is ${array1.at(index)}`);
